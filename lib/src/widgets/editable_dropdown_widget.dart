import 'package:flutter/material.dart';

class EditableDropdownWidget<T> extends StatelessWidget {
  const EditableDropdownWidget({super.key, required this.options});

  final List<T> options;

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
