import 'package:flutter/material.dart';

class GetCustomersPage extends StatelessWidget {
  const GetCustomersPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
